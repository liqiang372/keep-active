# keep-active 
keep your tools active when you are not 

## Usage
```bash
$ npm install -g keep-active

# and just type it
$ keep-active
```

## How does it work
Move your cursor a bit every second, use `Ctrl c` to terminate

